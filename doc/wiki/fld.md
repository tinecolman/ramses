# FLD

## Introduction
Flux Limited Diffusion (FLD) is a method for treating the radiation in RAMSES. It is an alternative to the M1 method.

## Dependencies
To use FLD with more than 1 radiation group, you will need LAPACK.
To install it locally on ubuntu:
```
sudo apt update
sudo apt install liblapack-dev libblas-dev
```

## Parameters