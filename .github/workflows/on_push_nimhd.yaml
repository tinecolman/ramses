name: Verify NIMHD

on:
  push:
    branches:
      - combo_fld_nimhd
      - isolate_nimhd_from_fld
      - nimhd_stage2

jobs:

  tests:
    name: Test Ramses NIMHD
    uses: ./.github/workflows/tests-nimhd.yaml
